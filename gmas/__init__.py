"""gmas — OpenClaw-style agent runtime for GraspMAS.

P0: telemetry + instrumented baseline evaluation.
Later phases add the runtime core (messages, registry, session, sandbox,
validator, audit) and the migrated agents.
"""
